greatest_common_divisor: λ( a, b )
  if a > b
    return gcd( a, b )
  else
    return gcd( b, a )

; Recursively apply Euclid's algorithm to find the GCD
; - requires that a is larger than b

gcd: λ( a, b )
  if b ≡ 0
    return a
  else
    return gcd( b, a % b )

greatest_common_divisor( 6,3)